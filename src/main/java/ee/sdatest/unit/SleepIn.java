package ee.sdatest.unit;

public class SleepIn {

    public boolean sleepIn(boolean weekday,
                           boolean vacation) {
        // 1. Step.
//        if (!weekday || vacation) {
//            return true;
//        }
       // return false;

        // 2. Step.
//        if (!weekday || vacation) return true;

        // 3. Step

        // if (!weekday || vacation) return true;
        // return false;

        // 4. Step
        return !weekday || vacation;
    }
}
